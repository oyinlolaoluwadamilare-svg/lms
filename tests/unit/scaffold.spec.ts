import { describe, expect, it } from "vitest";

// Placeholder proving the unit-test runner is wired (M0.1). Real domain logic
// (probability, weighting, staleness bands) arrives in M1+ per docs/07-build-backlog.md.
describe("scaffold", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
